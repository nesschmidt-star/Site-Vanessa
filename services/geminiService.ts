
// Este arquivo foi limpo pois a funcionalidade de IA foi removida do projeto.
// Mantido apenas para evitar erros de importação caso haja alguma referência residual.

export const generateDietPlan = async (): Promise<null> => {
  return null;
};

export const sendChatMessage = async (): Promise<string> => {
  return "";
};
